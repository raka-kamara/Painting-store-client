import { useParams } from "react-router-dom";

const UpdateMyPainting = () => {
    const { id } = useParams();
    console.log(id);

    
    return (
        <div>
            gdghfd
            
        </div>
    );
};

export default UpdateMyPainting;